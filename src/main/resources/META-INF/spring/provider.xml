<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
		http://www.springframework.org/schema/context  http://www.springframework.org/schema/context/spring-context-3.2.xsd
		http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

	<!-- 读取配置文件 -->
	<context:property-placeholder
		ignore-unresolvable="true" location="classpath*:/dubbo.properties" />

	<!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
	<dubbo:application name="${dubbo.application.name}" />

	<dubbo:registry protocol="zookeeper" address="127.0.0.1:2181" />

	<dubbo:protocol name="dubbo" server="netty" client="netty" 
		port="4094" accesslog="d:/access.log"  serialization="fst"/>

	<!-- <dubbo:protocol name="hessian" port="8888" /> -->


	<bean id="productService" class="com.demo.provider.ProductServiceImpl">
	</bean>
	
	<dubbo:provider ></dubbo:provider>

	<dubbo:service interface="com.demo.inter.ProductService" protocol="dubbo"
		ref="productService" timeout="10000" version="1.0.5" retries="2"
		loadbalance="random" actives="1"/>
		
<!-- 	<dubbo:service interface="com.demo.inter.ProductService" protocol="hessian" path="service/product"
		ref="productService" timeout="10000" version="1.0.2" retries="2"
		loadbalance="random" actives="2" /> -->
</beans>